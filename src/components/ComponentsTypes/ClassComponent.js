import { Component } from "react";

class ClassComponent extends Component {
  render() {
    return (
      <div>
        <h1>THIS IS CLASS COMPONET</h1>
      </div>
    );
  }
}

export default ClassComponent;

 /*React, class components are a way to create and manage stateful components.
 Class components are ES6 classes that extend from React.Component. 
 They have their own internal state and lifecycle methods, which allow developers to manage component state, 
 handle side effects, and respond to lifecycle events.*/
